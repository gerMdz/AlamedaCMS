<form method="post" action="{{ path('group_celebration_delete', {'id': group_celebration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group_celebration.id) }}">
    <button class="btn">Delete</button>
</form>
