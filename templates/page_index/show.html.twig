{% extends 'base.html.twig' %}

{% block title %}PageIndex{% endblock %}

{% block body %}
    <h1>PageIndex</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ page_index.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ page_index.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('page_index_index') }}">back to list</a>

    <a href="{{ path('page_index_edit', {'id': page_index.id}) }}">edit</a>

    {{ include('page_index/_delete_form.html.twig') }}
{% endblock %}
