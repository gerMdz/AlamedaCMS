<form method="post" action="{{ path('admin_organization_delete', {'id': organization.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ organization.id) }}">
    <button class="btn">Borrar</button>
</form>
