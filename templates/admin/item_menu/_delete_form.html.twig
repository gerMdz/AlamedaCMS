<form method="post" action="{{ path('app_item_menu_delete', {'id': item_menu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item_menu.id) }}">
    <button class="btn">Delete</button>
</form>
