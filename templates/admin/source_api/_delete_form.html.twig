<form method="post" action="{{ path('app_source_api_delete', {'id': source_api.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ source_api.id) }}">
    <button class="btn">Delete</button>
</form>
