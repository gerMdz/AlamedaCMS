{% extends 'base.html.twig' %}

{% block title %}Ministerio{% endblock %}

{% block body %}
    <h1>Ministerio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ministerio.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ ministerio.nombre }}</td>
            </tr>
            <tr>
                <th>Referente</th>
                <td>{{ ministerio.referente }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ministerio_index') }}">back to list</a>

    <a href="{{ path('ministerio_edit', {'id': ministerio.id}) }}">edit</a>

    {{ include('ministerio/_delete_form.html.twig') }}
{% endblock %}
