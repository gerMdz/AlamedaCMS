<form method="post" action="{{ path('enlace_corto_delete', {'id': enlace_corto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enlace_corto.id) }}">
    <button class="btn">Delete</button>
</form>
