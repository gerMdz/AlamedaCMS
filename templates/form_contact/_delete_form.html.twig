<form method="post" action="{{ path('app_form_contact_delete', {'id': form_contact.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ form_contact.id) }}">
    <button class="btn">Delete</button>
</form>
