{% extends 'contenido_base.html.twig' %}

{% block title %}Nuevo Brote{% endblock %}

{% block contenido_body %}
    <h5>Crear brote de página principal</h5>
<div class="form-container">
    {{ include('admin_brote/_form.html.twig') }}
</div>
    <a class="btn btn-link" href="{{ path('admin_brote_list') }}"><i class="fas fa-backspace"></i> Volver al listado de Brotes</a>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        $('.custom-file-input').on('change', function (event) {
            var inputFile = event.currentTarget;
            $(inputFile).parent()
                .find('.custom-file-label')
                .html(inputFile.files[0].name);
        });
    </script>
{% endblock %}
