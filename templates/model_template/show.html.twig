{% extends 'base.html.twig' %}

{% block title %}ModelTemplate{% endblock %}

{% block body %}
    <h1>ModelTemplate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ model_template.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ model_template.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ model_template.description }}</td>
            </tr>
            <tr>
                <th>Identifier</th>
                <td>{{ model_template.identifier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('model_template_index') }}">back to list</a>

    <a href="{{ path('model_template_edit', {'id': model_template.id}) }}">edit</a>

    {{ include('model_template/_delete_form.html.twig') }}
{% endblock %}
