<form method="post" action="{{ path('type_block_delete', {'id': type_block.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_block.id) }}">
    <button class="btn">Delete</button>
</form>
